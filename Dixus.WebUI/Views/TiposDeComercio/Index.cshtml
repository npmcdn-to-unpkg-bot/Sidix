@model IEnumerable<Dixus.Entidades.TipoDeComercio>

@{
    ViewBag.Title = "Index";
}

<h3>Index</h3>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoDeComercioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoDeComercioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoDeComercioId })
        </td>
    </tr>
}

</table>
